// Central API configuration — used by all frontend pages
// In production, set NEXT_PUBLIC_API_URL in Vercel environment variables
export const API_BASE = process.env.NEXT_PUBLIC_API_URL || "http://localhost:5002";
