"use client";
import { useTheme } from "@/context/ThemeContext";

// Superseded by ThemeStudio — kept for backward compatibility
// Opens the new Theme Studio panel instead
export default function ThemeSelector() {
  return null; // ThemeStudio renders via layout.tsx
}
