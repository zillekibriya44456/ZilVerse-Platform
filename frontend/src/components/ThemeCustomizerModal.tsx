"use client";
import { useTheme } from "@/context/ThemeContext";

// Superseded by ThemeStudio — kept as a thin wrapper for backward compatibility
export default function ThemeCustomizerModal() {
  // This component is no longer used directly.
  // Theme customization is handled by ThemeStudio (opens via Navbar palette button or floating button)
  return null;
}
